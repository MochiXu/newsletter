.env
.claude/

# Python
__pycache__/
*.pyc
.venv/

# 原始数据层(parquet):latest 入库(全量快照),history/features 可重建 → 忽略
data/raw/history/
data/features/

# 新闻抓取缓存(含文章全文,仅体积:不入库)
data/news_cache/
# 新闻语料库(article-level parquet,含 body,日积月累:体积大,不入库;v1.8)
data/news/
