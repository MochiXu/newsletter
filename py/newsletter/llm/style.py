"""通用文本风格约束(prompt 片段)。

所有产出中文文本的 LLM 调用共用(四层简报 + 新闻分类),表达"意图";
最终一致性由 `textnorm.normalize_text` 在落库前确定性地保证(prompt 只是减少后处理改动量)。
"""

from __future__ import annotations

TEXT_STYLE = (
    "【文本风格】所有文字字段统一遵守:"
    "(1) 标点一律用英文半角 . , ; : ! ? ( ),不要用中文全角句号/逗号/分号/顿号/括号;"
    "(2) 中文与半角字母或数字之间留一个空格(如 '较 MA200'、'升 9bp 至 2.23%'),"
    "但 9bp、0.27%、2s10s、MA200 等本身由字母数字组成的词,其内部不加空格;"
    "(3) 需要引用时用英文单引号 '...',绝不用英文双引号(会破坏 JSON)。"
)
